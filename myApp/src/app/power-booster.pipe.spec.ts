import { PowerBoosterPipe } from './power-booster.pipe';

describe('PowerBoosterPipe', () => {
  it('create an instance', () => {
    const pipe = new PowerBoosterPipe();
    expect(pipe).toBeTruthy();
  });
});
